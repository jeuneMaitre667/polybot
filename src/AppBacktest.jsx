import App from './App.jsx';

/**
 * Même dashboard que l’app principale (Solde & Performance, cartes du haut, puis stratégie).
 * Servi par `npm run dev:backtest` — seul le port / `index.backtest.html` diffèrent.
 */
export default App;
